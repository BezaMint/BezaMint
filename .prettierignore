node_modules/
.turbo/
.next/
dist/
target/
*.wasm
pnpm-lock.yaml
coverage/
*.toml
contracts/*/test_snapshots/

# Runtime output of scripts/seed-testnet-activity.sh: a record of what was seeded
# on chain, rewritten on every run, so it is not source and has no stable format.
demo/seed-manifest.json

# Generated by scripts/generate-error-catalog.py from the contracts' Rust error
# enums. The generator is their formatter and `pnpm run contract:errors:check`
# compares its output against what is committed, byte for byte, so a formatter
# pass here would only fight the generator.
docs/error-codes.md
apps/web/src/lib/contractErrors.ts
